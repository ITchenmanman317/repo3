#配置数据库相关信息
spring:
  datasource:
    druid:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://127.0.0.1:3306/ssm_db?serverTimezone=UTC
        username: root
        password: root
